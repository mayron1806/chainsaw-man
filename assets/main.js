function showCardSpoilerAlert(element) {
    element.querySelector(".spoiler").classList.add("on");
}
function hideCardSpoilerAlert(element) {
    element.querySelector(".spoiler").classList.remove("on");
}